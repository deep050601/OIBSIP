# BMI Calculator

A simple Python command-line application that calculates a user's **Body Mass Index (BMI)** based on their weight and height. The program validates user input, calculates the BMI, classifies the result according to standard BMI ranges, and allows users to perform multiple calculations without restarting the application.

## ✨ Features

* Calculates BMI using the standard formula.
* Displays BMI rounded to two decimal places.
* Classifies BMI into:

  * Underweight
  * Normal
  * Overweight
  * Obese
* Validates numeric input.
* Rejects zero and negative values.
* Handles invalid input using exception handling.
* Allows users to calculate BMI multiple times in a single session.

## 🛠️ Technologies Used

* Python 3

## 📚 Concepts Practiced

* Functions
* Loops (`while`)
* Nested loops
* Conditional statements (`if`, `elif`, `else`)
* Exception handling (`try` / `except`)
* Input validation
* Return values
* Formatted string output (f-strings)

## 🚀 How to Run

1. Make sure Python 3 is installed.
2. Download or clone this repository.
3. Open a terminal in the project folder.
4. Run:

```bash
python bmi_calculator.py
```

## 📖 BMI Formula

**BMI = Weight (kg) / Height² (m²)**

## 🎯 Future Improvements

* Add a graphical user interface (Tkinter).
* Store previous BMI calculations.
* Display health recommendations based on BMI.
* Support both metric and imperial units.

## 👨‍💻 Author

**Deep Savani**

Learning Python through practical projects.


